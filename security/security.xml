<odoo>
    <record id="module_category_real_estate_brokerage" model="ir.module.category">
        <field name="name">Real Estate</field>
        <field name="description">Livello di accesso per il modulo Real Estate</field>
    </record>

    <record id="estate_group_user" model="res.groups">
        <field name="name">Agent</field>
        <field name="category_id" ref="module_category_real_estate_brokerage"/>
    </record>

    <record id="estate_group_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="category_id" ref="module_category_real_estate_brokerage"/>
        <field name="implied_ids" eval="[(4, ref('estate_group_user'))]"/>

        <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>

    <record id="estate_property_rule_agent" model="ir.rule">
        <field name="name">Agent: vedi solo le tue proprietà o quelle libere</field>
        <field name="model_id" ref="model_estate_property"/>
        <field name="groups" eval="[(4, ref('estate_group_user'))]"/>
        <field name="domain_force">
            ['|', ('salesperson_id', '=', user.id), 
                ('salesperson_id', '=', False)]
        </field>
    </record>

    <record id="estate_property_rule_manager" model="ir.rule">
        <field name="name">Manager: vedi tutte le proprietà</field>
        <field name="model_id" ref="model_estate_property"/>
        <field name="groups" eval="[(4, ref('estate_group_manager'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
    </record>
</odoo>